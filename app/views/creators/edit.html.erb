<h1>edit creator</h1>
<%= link_to "cancel", settings_creator_path(@creator.uuid)%>
<%= form_with model: @creator do |form| %>
    <%= form.label :about %>
    <%= form.rich_textarea :about %>
<% end %>
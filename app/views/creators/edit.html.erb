<h1>edit creator</h1>
<%= link_to "cancel", settings_creator_path(@creator.uuid)%>
<%= render "form", creator: @creator %>
<%= button_to "Delete Creator", delete_creator_path(@creator.id), method: :delete, data: {turbo_confirm: "Are you sure?"} %>

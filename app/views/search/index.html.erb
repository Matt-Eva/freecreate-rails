
<% content_for :head do %>
    <meta name="turbo-cache-control" content="no-cache">
<% end %>

<%= form_with url: "/search", method: :get do |form|%>
    <%= form.label :title %>
    <%= form.search_field :title, value: @title%>
    <%= form.submit "search", name: nil %>
<% end %>